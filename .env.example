# =============================================================================
# EXEMPLE DE CONFIGURATION - Copier en .env et remplir avec vos vraies valeurs
# =============================================================================

# Base de données
POSTGRES_DB=sqli_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# JWT
JWT_SECRET=changeme

# Backend
BACKEND_PORT=8080
SPRING_PROFILE=prod

# Frontend
FRONTEND_PORT=3000
NODE_ENV=production
VITE_API_URL=http://localhost:8080

# Environnement
ENVIRONMENT=production

# Images Docker (pour CI/CD)
BACKEND_IMAGE=registry.gitlab.com/your-project/backend:latest
FRONTEND_IMAGE=registry.gitlab.com/your-project/frontend:latest